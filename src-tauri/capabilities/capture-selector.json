{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "capture-selector",
  "description": "Capture commands belong to the bundled selector window.",
  "windows": ["selector"],
  "permissions": ["capture-selector"]
}
