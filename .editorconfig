# Formatting is enforced by Ruff, Biome and rustfmt; this file is what editors
# read before any of those run.
root = true

[*]
charset = utf-8
# LF everywhere, matching .gitattributes, so an editor and a checkout agree.
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true
indent_style = space
indent_size = 4

[*.{ts,tsx,js,jsx,json,jsonc,css,html,yml,yaml}]
indent_size = 2

[*.md]
# Two trailing spaces are a hard line break in Markdown.
trim_trailing_whitespace = false

[*.{bat,cmd,vbs}]
end_of_line = crlf

[*.{gguf,zip,png,ico}]
insert_final_newline = false
