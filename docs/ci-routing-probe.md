# CI routing acceptance probe

Temporary prose-only change to validate the optimized pull request checks.  
This line also exercises the repository's permitted Markdown hard break.
This probe is closed without merging after its check results are recorded.
